# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

`@flexops/elements` is the embeddable React widget library for FlexOps shipping. It provides drop-in components third parties can render on their own sites: **rate calculator, delivery promise, tracking, address validation, returns, and label creation**. Published to npm as `@flexops/elements`; current version `1.0.2`.

Distinct from `@flexops/sdk` (the headless TypeScript SDK): Elements ships **UI**; the SDK ships **functions**. Elements depends on Gateway HTTP endpoints and uses the customer's publishable key (à la Stripe Elements) — no server-side secret leaks to the browser.

## Build & Run Commands

```powershell
npm install                     # Install dependencies
npm run build                   # Build dist/ via Vite (ESM + CJS + types)
npm run test                    # vitest run
npm run typecheck               # tsc --noEmit
```

Output: `dist/index.mjs` (ESM), `dist/index.cjs` (CJS), `dist/index.d.ts` (types). Importers pick automatically via the `exports` map in `package.json`.

## Architecture

```text
Customer's web app (Next.js / CRA / Vite / plain ESM)
│
├── imports → @flexops/elements (this repo)
│             └─ React components calling Gateway via fetch
│
└── Gateway BFF (gateway.flexops.io) — only ingress; rate-limited per pub key
```

All Elements widgets call **FlexOps Gateway** (never VSCS or Integrations direct). The customer's publishable key authenticates browser-originated calls; sensitive operations (label create with billing implications) still flow through Gateway's idempotency and metering middlewares server-side.

## Key Directories

| Path | Purpose |
|---|---|
| `src/` | React components, hooks, types |
| `tests/` | Vitest specs |
| `dist/` | Build output (gitignored — generated by `npm run build`) |
| `tsconfig.build.json` | Production build TS config (strict) |
| `tsconfig.test.json` | Test TS config |
| `vite.config.ts` | Library build config |

## Publish

GitHub Actions handles npm publish via **OIDC trusted publishing** (no long-lived `NPM_TOKEN`). Bump the version in `package.json`, tag, push — the workflow does the rest.

## Related Repositories

| Repository | Purpose |
|---|---|
| **This repo** | `@flexops/elements` on npm — `BillEisenman/flexops-sdk-elements` |
| `@flexops/sdk` (Node SDK) | Headless TS SDK — sibling, `BillEisenman/flexops-sdk-node` |
| FlexOps Gateway | Backend the widgets call — `BillEisenman/FlexOpsGateway` |
| FlexOps Developer Docs | Hosts the Elements integration guide — `BillEisenman/FlexOpsDeveloperDocs` |
